# Laravel API base URL
# In development (npm run dev), leave this EMPTY — Vite proxy handles /api → http://127.0.0.1:8000
# In production, set this to your deployed Laravel domain, e.g.:
# VITE_API_URL=https://your-laravel-app.com
VITE_API_URL=http://127.0.0.1:8000
